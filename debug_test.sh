export MOCK_IMAGE_EXISTS_LOCALLY="true"; export MOCK_CONTAINER_EXISTS="true"; export MOCK_CONTAINER_RUNNING="true"; export MOCK_CONTAINER_NAME="container-here-test"; export BATS_TEST_MODE=1; source tests/helpers/docker_mock.bash; container_name="container-here-test"; if docker ps -a --format "{{.Names}}" | grep -q "^$container_name$"; then if docker ps --format "{{.Names}}" | grep -q "^$container_name$"; then docker stop "$container_name" >/dev/null 2>&1; stop_result=$?; if [ $stop_result -eq 0 ]; then echo "success"; else echo "failure"; fi; else echo "not_running"; fi; else echo "not_found"; fi
